spring:
  application:
    name: system-gateway
  profiles:
    active: dev
  main:
    web-application-type: reactive
  cloud:
    nacos:
      server-addr: 47.96.168.204:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml